let add = fn(a, b) { return a + b; };
let subtract = fn(a, b) { return a - b; };
let multiply = fn(a, b) { return a * b; };

let x = 10;
let y = 5;

puts("Addition:", add(x, y));
puts("Subtraction:", subtract(x, y));
puts("Multiplication:", multiply(x, y));